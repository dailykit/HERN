table:
  name: subscriptionOccurenceView
  schema: subscription
select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - isValid
    - id
    - isVisible
    filter: {}
  role: consumer
- permission:
    allow_aggregations: true
    columns:
    - isValid
    - isVisible
    - id
    filter: {}
  role: guest-consumer
