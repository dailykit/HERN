table:
  name: menu
  schema: onDemand
select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - id
    - data
    filter: {}
  role: consumer
- permission:
    allow_aggregations: true
    columns:
    - id
    - data
    filter: {}
  role: guest-consumer
